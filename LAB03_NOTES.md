# اللاب 03 — التهيئة وSilver والتحقق باستخدام dbt

معبأ وفق [قالب ملاحظات أدلة اللاب](templates/LAB_NOTES.md) من إعداد ميعاد المري. التفسير أدناه مستند إلى التقارير المقدمة؛ لم تُنشأ نتائج تشغيل بديلة.

## التعريف

الدفتر: `day02/STUDENT.ipynb`. تشغيل التهيئة: `05a3580c4cd04e5bb29aa655ab3e1966`. تشغيل Silver: `193c4ae1f50b4c8db9364a8e1015c18b`. تشغيل dbt: `2be5afa6e0fe49a5adb0bf91f568b715` في `outputs/dbt_validation_nh58vl02`، من 09:53:58 إلى 09:57:39 UTC بتاريخ 2026-09-16.

- المجموعة: `MASAR_SMALL_V1`؛ بصمة السجل: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- بيئة الحزمة: Colab؛ Spark 3.5.8؛ Delta 3.3.3. راجع [البيئة والأدلة](RUN_EVIDENCE.md) لتفريق المرصود عن المحدد في المتطلبات.
- معرّف تعديل Git وقت التشغيل غير موجود في الحزمة. يُسجل معرّف التسليم النهائي في رسالة التسليم بعد آخر تعديل، ولا يُستبدل بمعرّف تشغيل اللاب.

## ما نُفذ

تنفيذ جزأي 03a و03b، ثم مسار dbt منفصل. التهيئة احتفظت بـ144 سجل رحلة و6 سائقين و216 حدثًا. Silver انتقلت من 72 إلى 75 رحلة بعد الدفعة المتأخرة. تقرير dbt المستقل يسجل `PASSED_DBT_NATIVE`. الحقل `dbt_executed=false` في تقرير Silver يخص تشغيل Spark في ذلك التقرير؛ لا ينفي تشغيل dbt الموثق في ملفه المستقل.

## الأدلة والتفسير

| سيناريو Silver | وصول الرحلات في Bronze | رحلات Silver | نسخة Delta |
|---|---:|---:|---:|
| الأساس وإعادة الاستلام | 144 | 72 | 0 |
| إعادة المدخل نفسه | 144 | 72 | 0 |
| الدفعة المتأخرة | 147 | 75 | 1 |
| إعادة محاولة الدفعة نفسها | 147 | 75 | 1 |
| إعادة تسليم متعمدة | 150 | 75 | 1 |

إجمالي الأجور بعد التأخر 1,875.60 ريالًا. مراحل dbt هي 72، 72، 75، 75 رحلة؛ والإجماليات 1,794.60، 1,794.60، 1,875.60، 1,875.60. تسجل محاولة dbt 17 أمرًا، منها بوابة ما قبل الدمج، وفحص الحداثة، والبناء، والتوثيق. وصلت ملفات التوثيق الأصلية [index.html](reports/dbt/commands/documentation/target/index.html) و[manifest.json](reports/dbt/commands/documentation/target/manifest.json) و[catalog.json](reports/dbt/commands/documentation/target/catalog.json). يثبت فحص manifest وcatalog وجود ستة نماذج وثلاثة مصادر، وتوجد نتائج الأوامر الأصلية في [reports/dbt/commands](reports/dbt/commands).

**التنظيم في dbt:** تشير `source()` إلى جداول المصدر الموجودة، وتربط `ref()` النماذج ببعضها. تتدرج النماذج من `stg_trips` و`stg_drivers` إلى `int_trip_candidates` ثم `silver_trips` ثم `mart_city_daily`، مع فرع `stg_gps` المستقل. يستخدم الاستقبال نافذة مراجعة ثلاثة أيام على `_ingested_at`، لا على تاريخ وقوع الرحلة؛ وتُفحص مراجعات المصدر والتعارضات قبل الدمج.

**إجابات أسئلة اليوم الثاني:**

1. 144 سجل استقبال تمثل دفعتين للرحلات الـ72؛ يفصل المفتاح التجاري بين النسخ والرحلات.
2. تكرار مفتاح السائق قد يضاعف صفوف الربط وأجورها؛ يجب فحص التفرد قبل الربط.
3. تنسب رحلة وقعت في 1 يونيو ووصلت في 4 يونيو إلى يوم الحدث في تقرير الطلب، وإلى وقت وصولها في اختبار حداثة الاستقبال.
4. اختلاف الأجرة ليس تكرارًا مطابقًا؛ قد يكون تصحيحًا ذا مراجعة أعلى، أو تعارضًا يجب رفضه إذا تشارك رقم المراجعة نفسه.
5. تختلف هويات التشغيل وأرقام بعض معاملات Delta مع بقاء بصمات محتوى الأعمال متطابقة.
6. للرحلة الأساسية ثلاثة أحداث GPS؛ ربطها خامًا يكرر أجرتها ثلاث مرات. يُجمع عدد الأحداث بحسب الرحلة قبل الربط مع Silver الفريدة.
7. شرط تاريخ الحدث الأكبر من آخر تاريخ معالج قد يفقد الرحلة التاريخية المتأخرة.
8. أجرة الرحلة النهائية ووقت نهايتها ومدتها الفعلية ومسافتها النهائية وأحداثها اللاحقة لا تُستخدم خصائص لقرار يصدر عند بدايتها ما لم تكن معلومة وقت القرار.

## الإعادة والتعافي

فُحص تطابق ملفات لقطات dbt بين base/rerun وبين late/late_replay، مع مطابقة بصماتها الأربع. وصلت ملفات dbt المولدة وتطابق تقرير محاولتها بالبايت مع التقرير الذي روجع سابقًا. لا يمثل ذلك إعادة تشغيل مستقلة. لم يتضمن اختبار اليوم الثاني تصحيح اليوم الثالث، وكان `include_correction=false`.

## القرار والخطوة التالية

الاحتفاظ بمسار dbt منفصل للمقارنة وعدم تشغيل تحويلات اليوم الثاني فوق Silver المصححة لاحقًا. وصل ملف الانتقال لليوم الثاني ومجلد dbt الناجح؛ وصل الدفتر المنفذ بنتائج التهيئة وSilver وdbt؛ يبقى رابط مشاركة الأرشيف مطلوبًا. لا تُعاد أوامر dbt لمجرد توليد دليل تاريخي مفقود إذا كانت الملفات الأصلية موجودة.
