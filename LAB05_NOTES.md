# اللاب 05 — تدفق Kafka ونقاط التحقق

معبأ وفق [قالب ملاحظات أدلة اللاب](templates/LAB_NOTES.md) من إعداد ميعاد المري. التفسير أدناه مستند إلى التقارير المقدمة؛ لم تُنشأ نتائج تشغيل بديلة.

## التعريف

الدفتر: `day04/STUDENT.ipynb`. معرّف التشغيل الناجح `e831afa2054745278b37edeccead1c62`. الموضوع `masar-day04-e831afa2054745278b37edeccead1c62`، ونقطة التحقق `checkpoints/day04/gps_e831afa2054745278b37edeccead1c62`.

- المجموعة: `MASAR_SMALL_V1`؛ بصمة السجل: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- بيئة الحزمة: Colab؛ Spark 3.5.8؛ Delta 3.3.3. راجع [البيئة والأدلة](RUN_EVIDENCE.md) لتفريق المرصود عن المحدد في المتطلبات.
- معرّف تعديل Git وقت التشغيل غير موجود في الحزمة. يُسجل معرّف التسليم النهائي في رسالة التسليم بعد آخر تعديل، ولا يُستبدل بمعرّف تشغيل اللاب.

## ما نُفذ

تنفيذ أربع مراحل: base، restart، replay، late. التقرير يصرح بتنفيذ Kafka وSpark، ويحتوي 13 فحصًا ناجحًا. سبق التشغيل عائقان موثقان في المحادثة: غياب المكتبة/الوسيط ثم غياب موصل Kafka داخل Spark؛ يجب حفظ خلايا تهيئة Colab وعلاج تحميل الموصل في دفتر المستخدم النهائي.

## الأدلة والتفسير

| المرحلة | سجلات الوصول التراكمية | الأحداث الفريدة |
|---|---:|---:|
| base | 216 | 216 |
| restart | 216 | 216 |
| replay | 218 | 216 |
| late | 219 | 217 |

الدليل الرئيس `reports/day04_stream_latest.json`، وتقارير المنتج والمستهلك تحت `reports/day04/e831afa2054745278b37edeccead1c62/`.

**إجابات الأسئلة 1–4 لليوم الرابع:**

1. لا توجد رسائل جديدة في restart، وتستأنف نقطة التحقق الموضع السابق؛ فيبقى العدد 216. ظلت هوية الاستعلام `6d84031d-8fe2-4493-a434-af8bf85a5c23` ثابتة، وتغير `query_run_id` بين المراحل. إعادة اللاب كاملًا تنشئ موضوعًا ونقطة جديدين وفق دليل الدورة؛ ليست استئنافًا خفيًا لتجربة قديمة.
2. مفتاح النقل هو `(topic, partition, offset)`، ومفتاح الأعمال هو `event_id`. أضاف replay رسالتين جديدتين بأحداث معروفة، فزاد الوصول إلى 218 وبقيت الأحداث 216. حماية إعادة محاولة المنتج لا تلغي الإرسال الجديد المقصود من التطبيق.
3. الحدث المتأخر له معرف جديد رغم توقيته الأقدم؛ احتُفظ به فأصبحت الأحداث 217. يسجل التقرير أن تنفيذ Watermark وتحمل أعطال الوسيط والتوسع الموزع غير مثبتة بهذا اللاب.
4. اشتراك المعرف مع اختلاف المحتوى تعارض، وليس تكرارًا آمنًا. يجب فحص المحتوى/البصمة في النقل قبل اختيار حدث واحد لكل معرف؛ لا يكفي `dropDuplicates` لأنه قد يخفي الفرق في الموقع.

## الإعادة والتعافي

بقيت نقطة التحقق وهوية الاستعلام نفسيهما في المراحل الأربع، وتطابقت إزاحات المنتج والمستهلك. لا تشمل المراجعة الحالية إعادة إرسال رسائل؛ تم فحص التقارير وبصمات الملفات الموجودة. الأرشيف يحتوي نقاط Spark ولقطات Delta، لكنه لا يحتوي تخزين وسيط Kafka.

## القرار والخطوة التالية

الاحتفاظ بالعددين: الوصول للتتبع، والأحداث الفريدة للاستخدام التحليلي. إبقاء Kafka محليًا على 127.0.0.1 وعدم تصفير الإزاحات. يجب إرفاق دفتر اليوم الرابع المعدل بمخرجاته، وعدم استبداله بالنسخة المرجعية المنشورة للدورة.
