# اللاب 06 — جودة البيانات والعزل والحوكمة

معبأ وفق [قالب ملاحظات أدلة اللاب](templates/LAB_NOTES.md) من إعداد ميعاد المري. التفسير أدناه مستند إلى التقارير المقدمة؛ لم تُنشأ نتائج تشغيل بديلة.

## التعريف

الدفتر: `day04/STUDENT.ipynb`. معرّف الجودة `876c88d851db4dd1b33d70f6ffa1b546`. الإصدار المرصود Great Expectations 1.7.0. تقارير GX المحفوظة مؤرخة 2026-09-16.

- المجموعة: `MASAR_SMALL_V1`؛ بصمة السجل: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- بيئة الحزمة: Colab؛ Spark 3.5.8؛ Delta 3.3.3. راجع [البيئة والأدلة](RUN_EVIDENCE.md) لتفريق المرصود عن المحدد في المتطلبات.
- معرّف تعديل Git وقت التشغيل غير موجود في الحزمة. يُسجل معرّف التسليم النهائي في رسالة التسليم بعد آخر تعديل، ولا يُستبدل بمعرّف تشغيل اللاب.

## ما نُفذ

فحص لقطة موثوقة من 75 رحلة، ومرشح مختلط من 82 صفًا، ثم عزل المعيب وإعادة فحص 75 صفًا. يثبت التقرير تنفيذ GX، مع حفظ Data Docs لحالات trusted وmixed وrechecked. نجحت الفحوص التسعة العليا؛ وفشل المرشح المختلط في GX هو النتيجة المقصودة.

## الأدلة والتفسير

| الحالة | الصفوف | نتيجة GX |
|---|---:|---|
| trusted | 75 | ناجح |
| mixed | 82 | فاشل كما هو مطلوب |
| rechecked | 75 | ناجح |

عُزلت 7 صفوف: `INVALID_CITY` و`INVALID_DISTANCE` و`INVALID_DURATION` و`INVALID_FARE` و`INVALID_TIMESTAMP` و`MISSING_TRIP_ID` و`UNKNOWN_DRIVER`؛ صف واحد لكل سبب. نسبة الرفض 7/82 ≈ 8.54%. تقييم `mixed_policy.json` ليس تشغيل GX بذاته؛ يُثبت تشغيل GX بتقريره المنفصل وملفات نقاط الفحص.

**إجابات الأسئلة 5–8 لليوم الرابع:**

5. وجود 60 صفًا صالحًا لا يحقق اكتمال 75 رحلة. فحص العدد المتوقع والتسوية مع خط الأساس يمنع الاعتماد؛ لا تكفي صلاحية الصفوف أو انخفاض نسبة الرفض.
6. يجب فشل المرشح المختلط لأنه يتضمن العيوب السبعة. لا يُرقّى المرشح؛ تُحفظ المعزولات وتُعاد فحوص الدفعة على الصفوف الـ75 قبل اعتمادها. مثال عيب جذري: معرف سائق غير موجود في المرجع.
7. ساعة السيناريو `2026-06-04T03:04:00Z`. عمر الوصول 240 ثانية مقابل حد 300، وعمر الحدث 33,540 مقابل حد 43,200. هما فرقان محسوبان في السيناريو، وليسا قياس زمن شبكة حي. مسافة توزيع المدن صفر مع 25 رحلة لكل مدينة؛ تثبت تطابق التوزيع الفئوي مع خط أساس العينة، لا انعدام الانحراف في أي مجتمع حقيقي.
8. ينقل ZIP لقطات البيانات والتقارير وملفات GX ونقاط Spark، دون مخزن Kafka. أسماء المجلدات ونجاح GX لا يفرضان صلاحيات ولا يثبتان امتثالًا نظاميًا. راجع [GOVERNANCE.md](GOVERNANCE.md).

## الإعادة والتعافي

تساوت بصمة أعمال اللقطة المعاد فحصها مع Silver المصححة: `1321d375742d806a1f0fef82be9e2862af04f5d18f3a976792a6b1d9aa1b4383`. لم يُعدّل المصدر الموثوق، وحُفظ المرشح الفاشل والمعزولات للتفسير.

## القرار والخطوة التالية

اعتماد لقطة الـ75 رحلة المعاد فحصها فقط لليوم الخامس. قواعد العدد والعلاقات وحداثة البيانات مكملة لاختبارات الصف. حدود الأداء الإنتاجي والصلاحيات والامتثال موثقة بوصفها غير مثبتة.

## حالة مخرجات الدفتر

وصلت النسخة اللاحقة `(2)` من دفتر المستخدم بمخرجات محاولة جودة جديدة بعد استعادة 504 ملفات. تظهر تسعة فحوص ناجحة وسبعة صفوف معزولة و75 صفًا معتمدًا، مع Python 3.11.13. معرف المرشح الظاهر هو `6801045da5ed4b1ca794844ac78b38bc`. حُفظت المخرجات كما أُرفقت في [دفتر اليوم الرابع](day04/STUDENT.ipynb)، وحُفظ [كود الاستعادة والفحص](scripts/colab_recovery/README.txt).

تقارير GX المنشورة أعلاه تخص المحاولة الأصلية `876c88d851db4dd1b33d70f6ffa1b546`. وصل ZIP المحاولة الجديدة `day04_quality_recovery_20260916T180618912333Z.zip` وفُحصت ملفات GX وDelta الخاصة بها. أكدت القراءة المستقلة لملفات Parquet وجود 82 مرشحًا و7 معزولات و75 معتمدًا؛ طابقت المعزولات سياسة الرفض، وطابق المعتمدون بصمة الأعمال السابقة. بقيت تقارير المحاولتين منفصلة. [تفاصيل المحاولة الجديدة](reports/quality/reruns/6801045da5ed4b1ca794844ac78b38bc/README.md). [سجل المراجعة](reports/review/quality_rerun_notebook.json).
