# اللاب 01 — استقبال البيانات وطبقة Bronze

معبأ وفق [قالب ملاحظات أدلة اللاب](templates/LAB_NOTES.md) من إعداد ميعاد المري. التفسير أدناه مستند إلى التقارير المقدمة؛ لم تُنشأ نتائج تشغيل بديلة.

## التعريف

الدفتر: `day01/STUDENT.ipynb`. مساحة العمل: `outputs/day01_bronze_jmbv27q9`. معرّف مساحة العمل: `fada638ae42445c48cb8798146263fa8`. تاريخ التشغيل التفصيلي لكل خلية غير مرفق؛ ترتبط هذه المساحة بسجلات الدورة التشغيلية المؤرخة 2026-09-16.

- المجموعة: `MASAR_SMALL_V1`؛ بصمة السجل: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- بيئة الحزمة: Colab؛ Spark 3.5.8؛ Delta 3.3.3. راجع [البيئة والأدلة](RUN_EVIDENCE.md) لتفريق المرصود عن المحدد في المتطلبات.
- معرّف تعديل Git وقت التشغيل غير موجود في الحزمة. يُسجل معرّف التسليم النهائي في رسالة التسليم بعد آخر تعديل، ولا يُستبدل بمعرّف تشغيل اللاب.

## ما نُفذ

تنفيذ استقبال الرحلات والسائقين وأحداث المواقع وكتابة Delta، ثم إعادة استقبال ملف الرحلات عمدًا. التقرير `reports/bronze.json` يصرح بـ`engine_executed=true`، وتسجل فحوصه السبعة القيمة `true`. ملف فحص المصدر المستقل ودفتر المستخدم المنفذ لم يصلا ضمن الحزمة.

## الأدلة والتفسير

**الأعداد:** 144 سجل وصول للرحلات، و72 رحلة فريدة، و6 سائقين، و216 حدث موقع. يحتفظ Bronze بالاستلام الأول `base_001` وإعادة الاستلام `replay_002`، كل منهما 72 سجلًا. لا تعني 144 وصولًا وجود 144 رحلة أعمال.

**سؤال اليوم الأول 1 — معنى الصف:** سجل الوصول هو ظهور السجل في دفعة استقبال؛ رحلة الأعمال تُعرّف بـ`trip_id`؛ وحدث الموقع يُعرّف بـ`event_id`. يستخدم تقرير الأعمال الرحلات الفريدة، وتُستخدم سجلات Bronze للتتبع والمراجعة.

**السؤال 2 — المدن:** اختلاف المسافات وحالة الأحرف قد يقسم المدينة إلى مجموعات متعددة. يُحفظ المصدر كما هو في Bronze، وتوحّد التسمية في التهيئة/Silver. لا تتضمن هذه الحزمة تقرير فحص المصدر الخاص بالمستخدم لإسناد عدد تسميات جرى تعديلها؛ لذلك لم يُنقل العدد من دفتر الدورة المرجعي.

**السؤال 3 — تتبع قيمة:** المسار التوثيقي للرحلة `SYN_T0001` هو المصدر، ثم سجلها في كل دفعة وصول، ثم Silver، ثم جدول الوقائع. تحفظ حقول المصدر والبصمة ووقت الاستيعاب سبب وجود نسختين. وقت الاستيعاب يصف وصول البيانات؛ ووقت بداية الرحلة يصف الحدث. تقرير Bronze يثبت تطابق بصمتي حمولة الدفعتين، ويجب إرفاق مخرج صف الرحلة من دفتر المستخدم لإكمال المثال التفصيلي.

**السؤال 6 — أنواع النجاح:** سلامة الملف تثبتها بصمته وفحص ZIP؛ تنفيذ المحرك تثبته تقارير التنفيذ وملفات Delta المشار إليها؛ اكتمال التسليم يتطلب أيضًا الدفاتر والملاحظات وروابط المشاركة. لا يجعل مؤشر `day01_bronze_success.json` الدورة كاملة مكتملة؛ فهو مؤشر Bronze فقط.

## الإعادة والتعافي

بصمة الحمولة للرحلات متطابقة بين الدفعتين، مع بقاء عدد الأعمال 72. طابقت مراجعة الحزمة بصمة تقرير Bronze مع المؤشر، وملفات البيانات وسجلات Delta مع المراجع المحفوظة. لم تُعد هذه المراجعة تشغيل Spark.

## القرار والخطوة التالية

الاحتفاظ بوصول البيانات في Bronze، وتأجيل توحيدها وإزالة تكرارها إلى الطبقة المناسبة. لاستكمال الدليل: حفظ دفتر اليوم الأول وفحص المصدر وملف الانتقال لليوم الأول، مع إبقاء المصادر الاصطناعية ثابتة.
