# اللاب 07 — بناء Gold والتعافي من فشل إصدار

معبأ وفق [قالب ملاحظات أدلة اللاب](templates/LAB_NOTES.md) من إعداد ميعاد المري. التفسير أدناه مستند إلى التقارير المقدمة؛ لم تُنشأ نتائج تشغيل بديلة.

## التعريف

الدفتر: `day05/STUDENT.ipynb`. البناء السليم الأول `60419550db4345efaae231d3bf8c04d8`، والبناء المختار بعد التعافي `6a5c31f6be704c46b0365b3a37b9fbbf`. محاولة الفشل المقصود موثقة في `reports/day05_failures/80b105e7d60d48508afdc098f54f5401.json`.

- المجموعة: `MASAR_SMALL_V1`؛ بصمة السجل: `20a7e45bed2980b9394c10e8532da3b9f40f614366bb2df26a88610253e768e3`.
- بيئة الحزمة: Colab؛ Spark 3.5.8؛ Delta 3.3.3. راجع [البيئة والأدلة](RUN_EVIDENCE.md) لتفريق المرصود عن المحدد في المتطلبات.
- معرّف تعديل Git وقت التشغيل غير موجود في الحزمة. يُسجل معرّف التسليم النهائي في رسالة التسليم بعد آخر تعديل، ولا يُستبدل بمعرّف تشغيل اللاب.

## ما نُفذ

بناء الجداول الثمانية، وإحداث فشل مقصود في مرشح، والتحقق من بقاء الإصدار السابق مختارًا، ثم إعادة بناء سليم بهوية جديدة. نجحت فحوص التعافي الأربعة. أُجري سيناريو التعافي في جلسة Spark نفسها وفق التقرير.

## الأدلة والتفسير

**إجابات الأسئلة 1–4 لليوم الخامس:**

1. يمثل صف `bi.fact_trips` رحلة واحدة، لذا يضم 75 صفًا؛ ويمثل صف `gold.driver_daily` سائقًا وتاريخ بداية محليًا، لذا يضم 18 مجموعة. يجب أن تتساوى مجاميع المقاييس: 75 رحلة، و1,880.60 ريالًا، و95,400 ثانية. `zone_resolution=city_proxy` مدينة تمثيلية؛ ليست حدود خدمة أو أحياء معتمدة.
2. ربط GPS الخام يكرر الأجرة بحسب عدد أحداث الرحلة. جُمعت الأحداث لكل رحلة قبل الربط، وأُبقيت الرحلات الثلاث بلا GPS بعدد أحداث صفر. يوجد 217 حدثًا فريدًا مقابل 219 رسالة نقل بعد إعادة الإرسال المتعمدة.
3. بعد الفشل المقصود يبقى البناء الأول السليم متاحًا حتى اكتمال البناء البديل. يحدد `reports/day05_gold_latest.json` سجل الإصدار المختار، ولا تكفي ملفات مرشح جزئي لاعتماده. حُفظ تقرير الفشل بدل اعتباره نتيجة نهائية.
4. اختلاف معرفي البناء يثبت إنشاء إصدارين، وتطابق بصمات المحتوى يثبت ثبات النتائج المقارنة. كل جدول Delta يعتمد بمعاملة مستقلة؛ لا تثبت التجربة معاملة واحدة للجداول الثمانية أو سلامة النشر المتزامن أو التعافي من انقطاع الجهاز.

| الجدول | الصفوف |
|---|---:|
| `gold.zone_hourly_demand` | 75 |
| `gold.driver_daily` | 18 |
| `bi.dim_zone` | 3 |
| `bi.dim_driver` | 6 |
| `bi.dim_date` | 3 |
| `bi.fact_trips` | 75 |
| `ai.zone_hourly_features` | 3 |
| `ai.zone_hourly_labels` | 3 |

يقرأ الإصدار المختار 75 رحلة من لقطة الجودة و217 حدثًا من لقطة التدفق. طابقت المراجعة بصمتي تقريري المدخلات وبصمة `release.json` مع المؤشر.

## الإعادة والتعافي

سجل `day05_recovery.json` وقوع الفشل المقصود وبقاء الإصدار السابق وتغير هوية إعادة البناء وتساوي المحتوى. هذه أدلة تعافٍ محلي في جلسة واحدة؛ ليست اختبار نشر مستقل في بيئة جديدة.

## القرار والخطوة التالية

قراءة الإصدار الذي يشير إليه المؤشر المعتمد، وعدم تخمين أحدث مجلد أو تعديل المؤشر يدويًا. عند بناء إصدار جديد عمدًا يجب إعادة اللاب 08 عليه وحفظ أرشيف جديد؛ لا داعي لإعادة البناء لمجرد إعداد هذا التوثيق.
